{
  "name": "mise-kitchen",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "if [ -d frontend ]; then (cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/dist ./dist); else (npm install && npm run build); fi",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5"
  }
}
